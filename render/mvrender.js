// const lens = document.getElementById('lens')

// async function updateLensPosition() {
//   const pos = await window.api.getCursorPosition()
//   lens.style.left = (pos.x - 150) + 'px'
//   lens.style.top = (pos.y - 100) + 'px'
// }

// setInterval(updateLensPosition, 16)